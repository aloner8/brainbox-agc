server {
    listen 80;
    server_name _;   # 👈 wildcard รับทุก host

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
